Graphics.Handlers.CanvasLoadedHandler = class CanvasLoadedHandler extends Handler {
    constructor(dispatcher) {
        super(dispatcher);        
    }

    handle(canvasLoadedEvent){
       
    }
}